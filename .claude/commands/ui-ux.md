Run the `ui-ux-pro-max` skill from `.claude/skills/ui-ux-pro-max/SKILL.md`.

Default workflow:
1. Generate design system recommendations first
2. Add domain-specific searches (style, color, typography, UX, chart) as needed
3. Apply stack guidance (default `html-tailwind` if unspecified)

When implementing changes in TUGON, preserve project design tokens unless explicitly requested otherwise.
