Run the `migrate` skill from `.claude/skills/migrate/SKILL.md`.

Supported usage:
- `/migrate status`
- `/migrate deploy`
- `/migrate <migration-name>`

Always validate schema first when creating migrations, and always ask for confirmation before running migration or deploy commands.
