Run the `test` skill from `.claude/skills/test/SKILL.md`.

Usage:
- `/test` for all server integration tests
- `/test <filename>` for a focused integration test

Use Node's built-in test runner through the existing npm scripts and report pass/fail with failure diagnostics.
